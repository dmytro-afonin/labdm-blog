# Content (`src/content/`)

Markdown for the **`posts`** collection lives in **`posts/`**. Configuration, including the Zod frontmatter schema and the `glob` loader, is in [`src/content.config.ts`](../content.config.ts) (see [Authoring posts](../../docs/authoring-posts.md)).
